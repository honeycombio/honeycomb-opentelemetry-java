version: '3.0'

services:
  collector:
    build:
      context: .
      dockerfile: collector.dockerfile
    volumes:
      - "data-volume:/var/lib"

  app:
    build:
      context: .
      dockerfile: app.dockerfile
    ports:
      - '5002:5002'
    depends_on:
      - "collector"

  bats:
    build:
      context: .
      dockerfile: bats.dockerfile
    volumes:
      - "data-volume:/var/lib"
    depends_on:
      - "app"

volumes:
  data-volume:
